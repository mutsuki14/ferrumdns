name: ci

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: test
        run: cargo test --all
      - name: check example configs
        run: |
          cargo run --quiet -- check -c examples/simple.yaml
          cargo run --quiet -- check -c examples/dev.yaml
          cargo run --quiet -- check -c examples/docker.yaml
          cargo run --quiet -- check -c examples/split-horizon.yaml
